syntax = "proto3";

option csharp_namespace = "ChatServiceGrpc";

package ChatService;

service ChatService {
  rpc ProccessConversation (stream SendMessage) returns (stream ReceiveMessage);
}

message SendMessage {
  string text = 1;
}

message ReceiveMessage {
  string username = 1;
  string text = 2;
}